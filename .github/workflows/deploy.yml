name: Deploy to Production

on:
  push:
    branches: [main]
    paths:
      - "ansible/**"
      - ".github/workflows/deploy.yml"

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Ansible with Docker collection
        run: |
          sudo apt-get update
          sudo apt-get install -y ansible
          ansible-galaxy collection install community.docker

      - name: Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Create SSH private key file
        run: |
          # Создаем приватный ключ из GitHub Secrets
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > private_key
          chmod 600 private_key
            
          # Проверяем что создался правильный приватный ключ
          echo "=== Private key content start ==="
          head -n 3 private_key
          echo "=== Private key content end ==="

      - name: Run Ansible playbook
        env:
          ANSIBLE_HOST_KEY_CHECKING: False
        run: |
          # Запускаем Ansible с указанием приватного ключа
          ansible-playbook -i ansible/inventory.ini ansible/playbook.yml \
            --private-key ./private_key \
            -e "dockerhub_username=${{ secrets.DOCKERHUB_USERNAME }}" \
            -e "dockerhub_token=${{ secrets.DOCKERHUB_TOKEN }}"
